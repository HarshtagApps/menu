/**
 * Flip these to show/hide UI features without hunting through screens.
 * Set `menuItemHighlightUx` to true to re-enable aurora + coachmark/focus-dim.
 */
export const FEATURE_FLAGS = {
  /** FoodItems double-tap coachmark + aurora; OrderItems focus aurora/focus-dim */
  menuItemHighlightUx: false,
};
